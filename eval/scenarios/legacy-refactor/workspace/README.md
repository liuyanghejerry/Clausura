# Eval fixture workspace

Seed data for the `legacy-refactor` eval scenario. The seeded issues are
intentional — do not "fix" them.

Legacy Python service with bare excepts, pickle deserialization, f-string
SQL, debug prints, and one hardcoded secret.
