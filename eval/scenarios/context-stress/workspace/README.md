# Eval fixture workspace

Seed data for the `context-stress` eval scenario.

- `data/records.json` — a large (≈330KB) JSON array; record `rec-1400`
  carries a seeded hardcoded API key near the tail. The seeded secret is
  intentional — do not remove it.
