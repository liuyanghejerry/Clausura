# Eval fixture workspace

Seed data for the `security-basics` eval scenario. The seeded issues are
intentional — do not "fix" them.

- `src/login.js` — SQL injection + hardcoded API key
- `src/render.js` — stored XSS via innerHTML
